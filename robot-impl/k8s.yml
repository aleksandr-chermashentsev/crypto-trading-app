apiVersion: apps/v1
kind: Deployment
metadata:
  name: "robot-impl"
spec:
  selector:
    matchLabels:
      app: "robot-impl"
  template:
    metadata:
      labels:
        app: "robot-impl"
    spec:
      containers:
        - name: "robot-impl"
          image: "gcr.io/robot-impl/jib-image"
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health/readiness
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health/liveness
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: "robot-impl"
spec:
  selector:
    app: "robot-impl"
  type: LoadBalancer
  ports:
    - protocol: "TCP"
      port: 8080